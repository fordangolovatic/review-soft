export enum EnvKeys {
	JWT_ACCESS_TOKEN_SECRET = 'jwt.accessToken.secret',
	JWT_ACCESS_TOKEN_EXPIRY = 'jwt.accessToken.expiry',
	JWT_REFRESH_TOKEN_SECRET = 'jwt.refreshToken.secret',
	JWT_REFRESH_TOKEN_EXPIRY = 'jwt.refreshToken.expiry',
	ENCRYPTION_ALGORITHM = 'crypto.algorithm'
}
