import { FavoriteArticleEntity } from './favorite-article.entity';

describe('FavoriteArticleEntity', () => {
  it('should be defined', () => {
    expect(new FavoriteArticleEntity()).toBeDefined();
  });
});
